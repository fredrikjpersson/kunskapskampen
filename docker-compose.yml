services:
  kunskapskampen:
    build: .
    container_name: kunskapskampen
    ports:
      - "8101:8101"
    environment:
      - PORT=8101
      - DB_PATH=/data/kunskapskampen.db
    volumes:
      - kk-data:/data
    restart: unless-stopped

volumes:
  kk-data:
