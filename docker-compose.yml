services:
  kunskapskampen:
    build: .
    container_name: kunskapskampen
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - DB_PATH=/data/kunskapskampen.db
    volumes:
      - kk-data:/data
    restart: unless-stopped

volumes:
  kk-data:
